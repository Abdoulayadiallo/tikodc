import 'package:flutter/material.dart';

class ParEmailPage extends StatelessWidget {
  const ParEmailPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
